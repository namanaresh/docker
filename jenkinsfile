pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/namanaresh/docker.git'
            }
        }
        stage('Hello World') {
            steps {
                echo 'Hello World from Jenkins + GitHub!'
            }
        }
    }
}
